
import { tableVersions } from './tableVersions';
import { formEntryVersions } from './formEntryVersions';

export const versions = {
  // Table versions
  ...tableVersions,
  
  // Form entry versions
  ...formEntryVersions
};
